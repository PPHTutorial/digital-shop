import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
Deno.serve(async req=>{try{const u=new URL(req.url);const tx=u.searchParams.get('transaction_id');const status=u.searchParams.get('status');const ref=u.searchParams.get('tx_ref');if(status!=='successful'||!tx||!ref)return Response.redirect(`${Deno.env.get('PUBLIC_SITE_URL')}/success.html`,302);const sb=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const vr=await fetch(`https://api.flutterwave.com/v3/transactions/${encodeURIComponent(tx)}/verify`,{headers:{Authorization:`Bearer ${Deno.env.get('FLW_SECRET_KEY')!}`}});const v=await vr.json();if(!vr.ok||v.data?.status!=='successful'||v.data?.tx_ref!==ref)return Response.redirect(`${Deno.env.get('PUBLIC_SITE_URL')}/success.html?status=failed`,302);const {data:o}=await sb.from('orders').select('id,amount,currency,status').eq('provider_reference',ref).single();if(!o)return new Response('Order not found',{status:404});if(Number(v.data.amount)!==Number(o.amount)||v.data.currency!==o.currency)return new Response('Payment mismatch',{status:400});const rawToken=crypto.randomUUID()+crypto.randomUUID();const hashBuf=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(rawToken));const hash=Array.from(new Uint8Array(hashBuf)).map(b=>b.toString(16).padStart(2,'0')).join('');await sb.from('orders').update({status:'paid',provider_transaction_id:String(tx),paid_at:new Date().toISOString(),download_token_hash:hash,download_expires_at:new Date(Date.now()+15*60*1000).toISOString()}).eq('id',o.id);return Response.redirect(`${Deno.env.get('PUBLIC_SITE_URL')}/success.html?token=${encodeURIComponent(rawToken)}`,302)}catch(e){return new Response(String(e?.message||e),{status:500})}});
